(function () {
  window.PrePa = window.PrePa || {};
  window.PrePa.firebase = window.PrePa.firebase || {};

  // Add Firebase web config here only when the client app needs public Firebase SDK access.
  // Keep privileged Firebase Admin credentials in Vercel environment variables and backend APIs.
  window.PrePa.firebase.config = window.PrePa.firebase.config || {};
})();
